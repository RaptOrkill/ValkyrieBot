{
  "name": "valkyriebot-worker",
  "version": "1.0.0",
  "description": "ValkyrieUHC Bot - Cloudflare Workers (HTTP Interactions) version",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy-commands": "node deploy-commands.js",
    "db:migrate": "wrangler d1 execute valkyriebot --remote --file=schema.sql"
  },
  "dependencies": {
    "discord-interactions": "^4.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "dotenv": "^16.4.5"
  }
}
